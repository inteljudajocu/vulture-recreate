_description:
  Article type and date

datePublished:
  _subscribed: publishTime
  _has:
    input: datepicker
    validate.required: true

keyTag:
  _label: Key tag
  _subscribe: keywords
  _has:
    input: simple-list
    help: An array of key tag


_groups:
  settings:
    fields:
      - keyTag
      - datePublished